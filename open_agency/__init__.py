"""Open Agency — an HR request API.

One verb, `request`. It consults the written company policy and usually says no.
The shape is the argument: HR rendered as `request(plea) -> Option[Rejection]`.
"""

__version__ = "0.1.0"
